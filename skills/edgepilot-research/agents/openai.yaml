interface:
  display_name: "EdgePilot Research"
  short_description: "Discover and backtest public strategies"
  default_prompt: "Use $edgepilot-research in my language. On the first reply, introduce EdgePilot Research and explain that its local Dashboard runs only while ChatGPT is open unless I explicitly enable long-term background operation. Guide me into the existing V2 matching flow and call recommend_strategies so ChatGPT can render the three cards; do not check or install the runtime unless I ask to backtest."
