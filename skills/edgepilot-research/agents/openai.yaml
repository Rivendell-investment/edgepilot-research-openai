interface:
  display_name: "EdgePilot Research"
  short_description: "Discover and backtest public strategies"
  default_prompt: "Use $edgepilot-research in my language. Before onboarding, call verify_activation; when ready, open the Research Dashboard immediately if requested, show its verified local link, and ask the first V2 preference question in the same reply. Continue one question at a time and call recommend_strategies after confirmation so ChatGPT can render the three cards. Require a Codex restart only when verify_activation reports restart_required; do not check or install the runtime unless I ask to backtest."
